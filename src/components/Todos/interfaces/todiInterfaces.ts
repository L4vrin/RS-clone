export interface ITodo {
  text: string;
  isCompleted: boolean;
  id: string;
}

// export interface IProps {
//   todos: Array<ITodo>,
//   todo: ITodo,
//   deleteTodo?: (id: string) => void
// };
